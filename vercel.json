{
    "version": 2,
    "builds": [
        {
            "src": "backend/src/**/*.ts",
            "use": "@vercel/node"
        },
        {
            "src": "flextrack/package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "build"
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "backend/src/$1"
        },
        {
            "src": "/(.*)",
            "dest": "flextrack/build/$1"
        }
    ]
}